import 'package:flutter/material.dart';
import 'package:tsitatnik/app.dart';

void main() {
  // Функция main() - входная точка вашего приложения

  // runApp() - функция, которая запускает ваше приложение и передает корневой виджет
  runApp(TsitatnikApp());
}
